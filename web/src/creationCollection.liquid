---
layout: creationLayout
tags: 
 - creationCollection
pagination: 
  data: creation
  alias: creation
  size: 1
  addAllPagesToCollections: true 
eleventyComputed: 
  metaTitle: "{{ creation.title }}"
  metaDescription: "{{ creation.leading }}"
  metaImageUrl: "{{ creation.featuredImage | default: siteSettings.metaImageUrl }}"
  id: "{{ creation._id }}"
permalink: "{{ creation.fullSlug }}/"    
---